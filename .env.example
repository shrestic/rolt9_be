# Database
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=rolt9

# API
API_V1_STR=/api/v1
JWT_SECRET=change_me_32_random_bytes
JWT_ALGORITHM=HS256
JWT_TTL_MINUTES=10080
JWT_ISSUER=http://localhost:8000

# Frontend
FRONTEND_URL=http://localhost:5173

# Discord OAuth2 + Bot
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_BOT_TOKEN=
DISCORD_REDIRECT_URI=http://localhost:8000/api/v1/auth/discord/callback
BOT_INVITE_URL=

# Token encryption (Fernet, base64)
TOKEN_ENCRYPTION_KEY=

# Redis
REDIS_URL=redis://localhost:6379/0

# Feature integrations (để rỗng = tắt tính năng tương ứng, an toàn)
# Tavily — web_search / read_link của agent
TAVILY_API_KEY=
# football-data.org free tier — đồng bộ trận World Cup cho WC Predict (https://www.football-data.org/)
FOOTBALL_DATA_API_KEY=
